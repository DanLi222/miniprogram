<view class="brand" wx:for="{{manufacturer_list}}" bindtap="handleTap" id="{{index}}">
  <div class="imageContainer"><image class="brandImage" src="{{item.imagePath}}" mode="widthFix"></image></div>
  <text class="brandName">{{item.pingying}}</text>
</view>
