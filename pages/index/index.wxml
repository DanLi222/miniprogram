<view class="brand" wx:for="{{manufacturer_list}}" bindtap="handleTap" id="{{index}}">
  <image class="brandImage" src="{{item.imagePath}}"></image>
  <text class="brandName">{{item.manufacturer}}</text>
</view>


 